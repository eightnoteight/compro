#include <bits/stdc++.h>
#ifdef __mr__
	#include "prettyprint.hpp"
#endif
using namespace std;

int main(int argc, char const *argv[]) {
	cin.sync_with_stdio(0);cin.tie(0);
	int n;
	cin >> n;
	node *tmp = NULL;
	node **x = &tmp;
	for (int i = 0; i < n; ++i) {
		
	}

	
	return 0;
}
